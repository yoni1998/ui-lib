import { LabelFontStylePipe } from './label-font-style.pipe';

describe('LabelFontStylePipe', () => {
  it('create an instance', () => {
    const pipe = new LabelFontStylePipe();
    expect(pipe).toBeTruthy();
  });
});
