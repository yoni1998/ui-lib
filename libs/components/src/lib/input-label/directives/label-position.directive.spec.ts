import { LabelPositionDirective } from './label-position.directive';

describe('LabelPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new LabelPositionDirective();
    expect(directive).toBeTruthy();
  });
});
