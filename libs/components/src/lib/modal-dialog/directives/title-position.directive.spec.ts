import { TitlePositionDirective } from './title-position.directive';

describe('TitlePositionDirective', () => {
  it('should create an instance', () => {
    const directive = new TitlePositionDirective();
    expect(directive).toBeTruthy();
  });
});
