import { HeaderStyleRightDirective } from './header-style-right.directive';

describe('HeaderStyleRightDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderStyleRightDirective();
    expect(directive).toBeTruthy();
  });
});
