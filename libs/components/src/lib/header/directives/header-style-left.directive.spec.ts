import { HeaderStyleLeftDirective } from './header-style-left.directive';

describe('HeaderStyleLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderStyleLeftDirective();
    expect(directive).toBeTruthy();
  });
});
