import { SideHeaderDirective } from './side-header.directive';

describe('SideHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new SideHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
